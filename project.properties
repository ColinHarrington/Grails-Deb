version=1.3.7
grails.version=1.3.7
package.name=grails-${grails.version}
version.debian=1
download.name=grails-${grails.version}.zip
unzip.name=grails-${version}
#download.doc.name=grails-docs-${version}.zip
#unzip.doc.name=doc

# debian/ubuntu packages this package should depend on
depends=java5-sdk | java6-sdk | java-compiler
conffiles=/etc/bash_completion.d/grails_commands
